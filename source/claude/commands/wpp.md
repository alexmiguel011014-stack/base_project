---
# base_project:managed
description: Alias for the "what do you want to do now?" menu — same one shown automatically at session start / after a task. Use when the user wants to see it on demand.
---

Show the "what do you want to do now?" menu right now, on demand.

Read `~/.claude/base_project/references/command-menu.md` and render it verbatim — same
file, same content as the menu that shows up automatically at session start and after a
substantial task (see `CLAUDE.md`'s "WhatsApp-style" menu instruction). This command
exists so the user can call it up any time, not just at those two automatic moments.

$ARGUMENTS
