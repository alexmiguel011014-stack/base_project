{
	"name": "base_project",
	"version": "1.0.0",
	"private": true,
	"license": "MIT",
	"description": "Global config installer for Claude Code, Codex, and opencode — validation/build tooling only, not a published package.",
	"scripts": {
		"lint": "biome check .",
		"typecheck": "tsc --noEmit",
		"audit:prod": "npm audit --omit=dev --audit-level=high",
		"check:unused-deps": "node dev/scripts/check-unused-deps.js",
		"validate:plugins": "node dev/scripts/validate-plugins.js",
		"scan:skill": "node dev/scripts/scan-skill.js",
		"test": "node --test dev/tests/*.test.js",
		"verify": "npm run lint && npm run typecheck && npm run validate:plugins && npm run check:unused-deps && npm test && npm run audit:prod"
	},
	"dependencies": {
		"ajv": "^8.17.1",
		"ajv-formats": "^3.0.1",
		"graphify": "^1.0.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.6",
		"typescript": "^5.9.3"
	}
}
