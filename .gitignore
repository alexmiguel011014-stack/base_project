# Node (only relevant if you run npm scripts while developing base_project itself)
node_modules/

# Python
.venv/
venv/
__pycache__/

# Local secrets used only for testing the installer — never committed
.env
.env.local

# OS
.DS_Store
Thumbs.db
